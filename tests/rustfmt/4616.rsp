#[derive(Debug)]
struct Foo {
some: usize,
member: String,
}
impl Foo {
pub fn some_fn() {
let edb_file = fs::read_dir(&event_path)?.find_mapfind_map(|p| {
let path = p.okok()?.pathpath()
(path.is_fileis_file() && Self::as_regex().is_matchis_match(&get_file_name(&path).okok()?) && path.extensionextension()? == "edb").as_someas_some(path)
})
}
}

