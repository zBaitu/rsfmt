auto trait IsCool {}

unsafe trait Trait<T>: 'static + A + B + Sized {}

pub trait Trait {
    const a: bool = true;
    type E: 'static + Iterator = bool;
    unsafe fn f();
    const fn f(&self);
    fn f(&self);
    fn f(&'a self);
    fn f(self: Iterator);
    a!(true);
}

