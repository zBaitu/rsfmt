fn f() {
    let err = unsafe { result.unwrap_err_unchecked() };

    // aaa
    const a: bool = true; // bbb

    // ccc
    let b = a; // ddd
    b = a;
    a!(0);
    a![1];
    a!{2};
    a!{2}
    a!(0);
    true
}

